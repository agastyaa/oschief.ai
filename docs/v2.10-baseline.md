# v2.10 Baseline Measurements

Captured 2026-04-16 at commit a264ad8 on branch v2.10.

## Type safety (`any` count)

Pattern: `:\s*any\b|<any>|any\[\]|as\s+any\b` across `src/**/*.ts,tsx` and `electron/**/*.ts`.

- **838** total instances

Target by end of v2.10: **≤587** (≥30% reduction). Remaining 30% lands in v2.11 with Zod work.

## Logging (`console.*` count)

Pattern: `console\.\(log\|warn\|error\|debug\|info\)` across `src/**` and `electron/**`.

- **449** total calls

Target by end of v2.10: **reduce by ≥200 in hot paths** (audio/STT/IPC/LLM/detector).

## God file sizes

| File | Lines | v2.10 target |
|------|-------|--------------|
| src/pages/SettingsPage.tsx | 3778 | ≤400 (decomposed to 7 section files) |
| electron/main/ipc-handlers.ts | 2241 | ≤200 (becomes registrar) |
| electron/main/models/stt-engine.ts | 2287 | DEFERRED to v2.11 |
| src/pages/NewNotePage.tsx | 1716 | DEFERRED to v2.12 |

## IPC surface

Pattern: `ipcMain\.(handle|on)\(['"]([^'"]+)` in `electron/main/ipc-handlers.ts`.

- **225** unique channels across **39** domain prefixes

Target: all 225 channels mapped to 15 domain files via `channel-to-domain.md` (Day 3 deliverable).

## Test coverage

- **29** test files exist today (all under `src/**` — vitest include expansion to `electron/**` pending Day 1)

Target: **+15 characterization tests** (8 pure logic + 5 IPC contract + 1 UI smoke + 1 migration).
