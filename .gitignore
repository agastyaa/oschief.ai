# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
out
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Secrets and env (API keys, tokens — never commit)
.env
.env.*
!.env.example

# User/app data (lives in Electron userData, not in repo)
*.db
*.db-wal
*.db-shm
keychain.enc
**/secure/*.enc

# Optional provider bundles (distributed separately, not in repo)
electron/main/cloud/optional-providers-dist/

# External skill/code (optional — remove if you want to track it)
.claude/

# Cursor IDE
.cursor/
.gstack/

# Bundled AI models (downloaded at build time, not committed)
electron/resources/darwin/models/
electron/resources/darwin/syag-mlx-llm/.build/
electron/resources/darwin/parakeet-coreml/.build/
